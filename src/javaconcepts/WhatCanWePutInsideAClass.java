package javaconcepts;

/*
 * public: can be extended and can have static and instance fields and methods
 * abstract: can have abstract methods (methods without body)
 * final: final classes can not be extended
 */
public class WhatCanWePutInsideAClass { //<- outer classes can only be public, abstract and final.
	
	/*
	 * 	inside a class we can have: variables, methods, blocks, constructors, inner classes.
	 */
	
	
	public static class innerClass{ //<- inner classes can also  be static
		/*
		 * Inner class is a nested class and can be declare within the outer class 
		 */
	}
}
